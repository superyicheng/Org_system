# Org_system demo prompts

# Use with recall_experience as consumer Tom
How do I make the Postia placenta iDynoMiCS 2 validation config grow spatially?

# Capture this as a candidate, then verify it
The simulation completed after enabling FungalGrowthManager.updateBody and AgentRelaxation. Radial growth is now nonzero.

# A negative experiment remains visible to admins but must not be served as verified
We plan to embed 30 days of Kubernetes logs. Check whether an existing verified experience applies.
